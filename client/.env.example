# Vite proxies /api and socket connections to the backend during dev (see vite.config.js).
# No environment variables are required for local development.
# For production, set VITE_API_URL if your API is hosted on a different origin.
VITE_API_URL=http://localhost:5000
